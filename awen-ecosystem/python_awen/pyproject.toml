[project]
name = "awen_py"
version = "0.1.0"
description = "Python contracts and convenience wrappers for the AWEN compiler/runtime."
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
