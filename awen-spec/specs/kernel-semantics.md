Kernel Semantics v0.1

Defines kernel interface, composition rules, parameter binding, scheduling and execution model, and calibration hooks.

Kernels must declare:
- Ports and port types
- Parameter schema with bounds and tolerances
- Timing constraints (latency, coherence window)
- Measurement hooks and conditional outputs

TODO: Add API signatures and examples.
